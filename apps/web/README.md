# SyndeoCare web and API

## Getting Started

Run from the repository root:

```bash
pnpm dev
```

This application contains the Arabic/English public site, Supabase-authenticated
dashboard, and `/api/v1` backend-for-frontend used by Expo. See the root README
and `docs/` for architecture and deployment instructions.
